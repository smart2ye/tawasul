# منفذ تشغيل الخادم
PORT=3000

# مفتاح التشفير لجلسات المستخدمين ورموز الـ JWT (قم بتغييره لشيء معقد جداً)
JWT_SECRET=tawasul_secret_key_2026_super_safe_key_123

# رابط قاعدة بيانات PostgreSQL السحابية (اختياري)
# في حال عدم توفيره، سيقوم الخادم تلقائياً بإنشاء قاعدة بيانات SQLite محلية باسم database.sqlite
# DATABASE_URL=postgres://user:password@hostname:5432/dbname
